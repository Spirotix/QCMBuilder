Les classes avec le même nom peuvent exister dans différents paquetages pour éviter les conflits de noms. Le paquetage java.lang est automatiquement importé par défaut dans tous les programmes Java.
Une classe ne peut appartenir qu’à un seul paquetage, et un fichier ne peut contenir qu’une seule déclaration de paquetage.
