- Implements est le mot-clé utilisé pour indiquer qu'une classe implémente une interface.
- Une méthode par défaut (default) fournit une implémentation dans une interface, introduite à partir de Java 8.
- Une classe abstraite peut contenir des méthodes avec ou sans corps.
- Une interface permet à une classe de suivre plusieurs comportements, ce que l'héritage multiple classique en Java ne permet pas.

